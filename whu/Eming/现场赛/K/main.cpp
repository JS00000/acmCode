#include <iostream>
#include <cstdio>
using namespace std;

int main()
{
    long l,w,A;
    while  (~scanf("%d %d %d",&l,&w,&A))
    {
    
        if ((a-b)%3==0)
        {
            ans = a+b-3;
        }
        else
        {
            ans = a+b-2;
        }
        if ((a==1)&&(b==1)) ans = 0;
        cout << ans << endl;
    }
    return 0;
}
