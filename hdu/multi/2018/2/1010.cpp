#include <cmath>
#include <cstdio>
#include <cstring>
#include <algorithm>
#define maxn 100007
using namespace std;

int n, x, y;
int a[maxn];

int main()
{
    while (~scanf("%d%d%d", &n, &x, &y)) {
        for (int i = 0; i < n; i++) 
            scanf("%d", a+i);
        for (int i = 1; i < n; i++) {
            int j = i;
            while ()
        }
    }
    return 0;
}
